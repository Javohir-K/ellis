import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Header from "@/components/Header";

export default function quiz() {
  return (
    <View>
      <Header title="Quiz and tests" />
    </View>
  );
}

const styles = StyleSheet.create({});
